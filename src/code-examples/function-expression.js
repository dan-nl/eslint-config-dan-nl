'use strict';

var foo = function foo( x, y, z ) {
  // do something
  console.log( x, y, z );
};

var bar = function ( x, y, z ) {
  // do something
  console.log( x, y, z );
};

foo( 1, 2, 3 );
bar( 1, 2, 3 );
